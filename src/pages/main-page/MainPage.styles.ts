import { SxProps, Theme } from "@mui/material";

export const pagination: SxProps<Theme> = {
    marginBottom: '40px',
    ul: {
        width: '100%',
        display: 'flex',
        justifyItems: 'center'
    }
  };